#include "Task3.h"

Task3_t stage, last_stage

void Task3Handler(void) {
    switch(stage) {
        case Turn1_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case WaitForOpenMV_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case FollowLine1_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case Turn2_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case FollowLine2_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case WaitForMedicineOut_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case Back_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case Turn3_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case FollowLine3_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case Turn4_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

        case FollowLine4_3rd:
            // stage initiate
            if(stage != last_stage) {
                last_stage = stage;

                // initiate todo

            }

            // loop

            // end condition
            if(1) {
                // end todo

                stage ++;
            }
            break;

    }
}
